#include <iostream>
#include <vector>
#include <algorithm> 
#include "FirstTask.h"
#include "SecondTask.h"
#include "ThirdTask.h"
//FROM FIRSTTASK.H
std::vector<int> vozvedenie(int input_1, std::vector<int> vec);
int checkInput(int input_1);
// FROM SECONDTASK.H
int returnQuotient(int dividend, int divisor);

// Задание №1

int main() {
    /*
    int input_1;
    
    
    std::cout << "Введите положительное целое число больше 64: ";
    std::cin >> input_1;

   
    if (checkInput(input_1 == 0))
    {
        std::cout << "Число должно быть больше 64." << std::endl; 
        return 0;
    }

    std::vector<int> vec(1, 2); 

   vec = vozvedenie(input_1, vec);

   
    std::cout << "2 в степени " << input_1 << " равно ";
    for (int i = vec.size() - 1; i >= 0; --i) {
        std::cout << vec[i];
    }
    std::cout << "\n";

    return 0;

    */

    // Задание №2
    /*
    int divisor = 1000; 
    int dividend = 10;
    int result = returnQuotient(divisor, dividend); 
    std::cout << divisor << " / " << dividend << " = " << result;
    */
    
    //задание 3
    start(); 
    return 0;



    return 0;

}